{
  "kind": "collectionType",
  "collectionName": "xyzs",
  "info": {
    "name": "tripOptions",
    "description": ""
  },
  "options": {
    "increments": true,
    "timestamps": true,
    "draftAndPublish": true
  },
  "pluginOptions": {},
  "attributes": {
    "tripTypes": {
      "type": "enumeration",
      "enum": [
        "Adventure",
        "Relaxation",
        "Holiday",
        "Test"
      ]
    },
    "city": {
      "type": "enumeration",
      "enum": [
        "Hamburg",
        "Leverkusen",
        "London",
        "Islamabad"
      ]
    },
    "package": {
      "type": "enumeration",
      "enum": [
        "tier1",
        "tier2"
      ],
      "required": true
    },
    "roomType": {
      "type": "enumeration",
      "enum": [
        "Single",
        "Double",
        "Triple"
      ]
    },
    "departureDate": {
      "type": "json"
    }
  }
}
